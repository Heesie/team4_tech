<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create account</title>
    <link rel="stylesheet" href="/styles/basis.css">
</head>
<body>

    <%- include('header') %>

    
    <section class="wrapper">
        <main class="mainForm">
            <h2>Create account</h2>
            <p>Password must be at least 8 characters, 1 uppercase letter, and 1 number</p>

                <form action="/createAccount" method="POST">
                    <% if (errorMessage) { %>
                        <div class="error">
                            <%= errorMessage %>
                        </div>
                    <% } %>

                    <label for="username">Username</label>
                    <button type="button" onclick="generateRandomUsername()">Generate a random name</button>
                    <input type="text" id="username" name="username" placeholder="Username" value="<%= username %>" required/>
                            
                    <label for="email">Email address</label>
                    <input type="email" id="email" name="email" placeholder="Email address" value="<%= email %>" required/>

                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" placeholder="Password." required>

                    <label for="passwordConfirm">Confirm password</label>
                    <input type="password" id="passwordConfirm" name="passwordConfirm" placeholder="Confirm password" required>

                    <input type="submit" value="Create account">
                </form>

                <p>Already have an account? <a href="/login">Log in</a></p>
                    
        </main>
    </section>
    
<style>


#button-login {
    background: #578A37;
    color: white;
    border: none;
    padding: 12px;
    font-size: 16px;
    font-weight: bold;
    border-radius: 5px;
    cursor: pointer;
    transition: background 0.3s;
  }
  
  #button-login:hover {
    background: #2C5810;
  }
  
  
  
  
  

  
  .wrapper {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 90vh;
    width: 100%;
    margin-top: 1em;
    padding-bottom: 5em;
    padding-top: 5em;
    
  }
  
  /* account */
  
  /* inloggen en registreren functie  */
  .mainForm {
    width: 100%;
    max-width: 400px;
    background: var(--background-white);
    padding: 2em;
    border-radius: var(--border-radius-big);
    box-shadow: var(--box-shadow);
    
  }
  
  .mainForm form {
    display: flex;
    flex-direction: column;
  }
  
  .mainForm label {
    font-size: 1em;
    margin-bottom: 0.5em;
    font-weight: bold;
  }
  
  .mainForm input {
    padding: 0.8em;
    margin-bottom: 1.2em;
    border: var(--color-input-border);
    border-radius: var(--border-radius-small);
    font-size: 1em;
    transition: 0.3s;
  }

  .mainForm button{
    background-color: var(--background-recept-random-username);
width: 50%;
    margin-bottom: 1em;
    border-radius: var(--border-radius-small);
    transition: 0.3s;
    border: none;
    padding: 10px 20px;
    color: var(--text-color-button);

  }
    

  @media (max-width:700px){
    .mainForm{
      width: 70%;
    }
  }



  .mainForm input:focus {
    outline: none;
    border-color: var(--color-input-border);
  }
  


  .mainForm input:focus::placeholder {
    color: var(--color-text-desktop);
  }


  .mainForm input:hover {
    color: var(--color-text-desktop);
    border: var(--color-input-border-hover);
  }

      
  .mainForm input:hover::placeholder{
    color: var(--color-text-desktop);
  }

  
  .mainForm input[type="submit"] {
    background-color: var(--background-color-button);
    color: var(--text-color-button);
    cursor: pointer;
    font-weight: bold;
    transition: 0.3s;
  }
  
  
  .mainForm input[type="submit"]:hover {
    background-color: var(--background-color-button-hover);
  }

      
  .mainForm p {
    text-align: center;
    font-size: 0.9em;
    margin-top: 1.5em;
    color: var(--text-color-gray);
  }


  
  .mainForm a {
    color: var(--link-color-text);
    text-decoration: none;
    font-weight: bold;
  }
  
  .mainForm a:hover {
    text-decoration: underline;
  }
  
  .mainForm .error {
    color: var(--error-color-text);
    text-align: center;
    margin-bottom: 1em;
    font-weight: bold;
  } 




      /* animation bounche for fun */

      @keyframes slowBounce {
        0%, 100% {
            transform: translateY(0);
        }
        50% {
            transform: translateY(-8px);
        }
    }
    
    .wrapper {
        animation: slowBounce 3s ease-in-out infinite;
    }


</style>
    <script>
        // Generate random username function
        function generateRandomUsername() {
            const username = 'user' + Math.floor(Math.random() * 1000);
            document.getElementById('username').value = username;
        }
    </script>
</body>
</html>