<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create account</title>
    <link rel="stylesheet" href="/styles/style.css">
</head>
<body>

    <%- include('header') %>

    <section class="wrapper">
        <main class="mainForm">
            <h1>Create account</h1>

                <form action="/createAccount" method="POST">
                    <% if (errorMessage) { %>
                        <div class="error">
                            <%= errorMessage %>
                        </div>
                    <% } %>

                    <label for="username">Username</label>
                    <input type="text" id="username" name="username" placeholder="Username..." value="<%= username %>" required/>
            
                    <!-- Genereer een nieuwe gebruikersnaam -->
                    <button type="button" onclick="generateRandomUsername()">Generate random username</button>
                    
                    <label for="email">Email address</label>
                    <input type="email" id="email" name="email" placeholder="Email address..." value="<%= email %>" required/>

                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" placeholder="Password..." required>

                    <label for="passwordConfirm">Confirm password</label>
                    <input type="password" id="passwordConfirm" name="passwordConfirm" placeholder="Confirm password..." required>

                    <input type="submit" value="Create account">
                </form>

                <p>Already have an account? <a href="/login">Log in</a></p>
                    
        </main>
    </section>
    
        <%- include('footer') %>

    <script>
        // Genereer een willekeurige gebruikersnaam
        function generateRandomUsername() {
            const username = 'user' + Math.floor(Math.random() * 1000);
            document.getElementById('username').value = username;
        }
    </script>
</body>
</html>